# 复制为 .env 后按需修改
# E2E 测试目标服务地址（frontend 入口，playwright 通过它访问页面与 /api 代理）
E2E_BASE_URL=http://localhost:3500

# E2E 登录密码。数据库未初始化时 global-setup 以它自动初始化管理员；
# 复用已有开发库时它必须与库中现有用户密码一致（例如本地 dev 库为 admin123）
E2E_LOGIN_PASSWORD=admin