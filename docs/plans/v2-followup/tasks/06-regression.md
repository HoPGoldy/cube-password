# T06: 全量回归

## 目标

确认 v2 收尾后全部功能符合 context.md 第 4 节完成定义。

## 上下文

- context.md 第 4 节端到端验收（完成定义 + 全量命令）。
- 冒烟路径：备份本地库后用全新库走一遍（init → 登录 → 建 Password 锁分组 → 正/误密码解锁 → 改密码正确 hash 成功 / 无 hash 拒绝 → access-token 接口 404），再在迁移后的本地库走日常流程。
- push 后确认 CI verify + e2e 两个 job 全绿。

## 边界

无代码改动；只跑验证与修复回归中发现的问题（修复限于前序任务引入的回归）。

## 验收

context.md 第 4 节全部命令通过；完成定义逐条满足；CI 全绿。

## 依赖

T02、T03、T04、T05。
