# T06: 全量回归

## 目标

确认清理后功能行为与清理前完全一致（零行为变更是本 PRD 的验收底线）。

## 上下文

- context.md 第 4 节端到端验收（全量命令）。
- 手工冒烟路径：登录 → 增删改查凭证 → 改密码 → 分组增删/解锁 → TOTP 配置页。
- 本地开发库跑 e2e 前确认 `E2E_LOGIN_PASSWORD` 与库内密码一致（见 `packages/e2e/.env` 注释）。

## 边界

无代码改动；只跑验证与修复回归中发现的问题（修复限于前序任务引入的回归）。

## 验收

context.md 第 4 节全部命令通过（含 `pnpm test:e2e` 45 用例全绿），功能无感知差异。

## 依赖

T02、T03、T04、T05。
